# A collection of scripts for backup and copying files from the git repo to the home directory.
 ### not complete yet
## backup.sh copies all the files from the home directory besides ~/backup into ~/backup into a folder named after the time of the backup

## copyfiles.sh will copy all the folders and files from the git directory into the respective folders in the home directory

## note
    There will probably also be a makefile to compile the c++ code from the git repo, and have the executable go into the correct directory within the home folder
